name: Validate static site data

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  event-data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/validate_events.py radar/events.json
